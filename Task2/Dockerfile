# Використовуємо офіційний базовий образ Python
FROM python:3.8

# Встановлюємо усі залежності, необхідні для додатку
RUN pip install poetry

# Створюємо робочу директорію для нашого додатку
WORKDIR /app

# Копіюємо файли залежностей та установлюємо їх
COPY pyproject.toml poetry.lock /app/
RUN poetry install

# Копіюємо всі файли додатку у робочу директорію контейнера
COPY . /app

# Встановлюємо команду, яка буде виконуватись при запуску контейнера
CMD ["poetry", "run", "python", "main.py"]
